{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "The bundled Market Me companion UI may call only the application's validated Rust commands and default core APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

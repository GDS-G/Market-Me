# Market Me implementation status

## Current implementation: 1.23.0 local preview, 2026-09-09

Market Me is still being built against the [approved conceptual specification](https://docs.google.com/document/d/1Iidc682xecSknAS8WeaNc1HH4V0ZvcKL_izEiU9Ng_k/edit), including its 23 child specification sections. The local review used the current `market-me-spec-current.md` extract in the task workspace. This document is a current capability/gap map, not a completion certificate. The specification's global acceptance requirements remain the target; implemented subsets do not replace them.

The preview prepares a non-executable Campaign and governed drafts directly from an approved package, then finalizes one exact approved text preview into a protected same-Campaign executable draft without JSON graph authoring. Separate exact-version publication and activation remain explicit; finalization grants neither approval nor sending. It builds on historical evidence retention, bounded scheduling, workspace views and governed publication infrastructure. Source setup, automatic ready-event template binding and the full folder-to-approved-draft-to-campaign experience remain unfinished. [Finalization contracts](CAMPAIGN_FINALIZATION.md), [Campaign preparation contracts](CAMPAIGN_PREPARATION.md) and [the remaining plan](CAMPAIGN_PREPARATION_PLAN.md) distinguish supported subsets from subsequent work.

Package/native metadata is 1.23.0. All 1,405 TypeScript tests across 101 files pass without skips, along with workspace typechecks/lint, all 112 migration checksums, both zero-vulnerability audits and companion native checks/three Rust tests/unsigned packaging. Authenticated browser and 94-page production build/smoke pass. Reviewed source `02d84a0531a01bb6a07f9d25d3292808b55189b3` passed clean Linux CI in 3 minutes 44 seconds with the same 1,405 cases and is published on main. Exact evidence belongs in [Releases](RELEASES.md) and [CI](CI.md). No installer distribution, hosted production deployment or native cross-platform acceptance is implied by the version number.

## Supported behavior and remaining work

The implementation pointers below locate relevant code; they do not by themselves prove an entire requirement. Where provider credentials, operating-system testing, representative evaluation, or production operation are missing, that acceptance remains open.

| Specification area | Implemented local capability | Incomplete or not yet verified |
| --- | --- | --- |
| Product experience; workspaces, accounts and roles (01–02) | OIDC/session framework, exact-email invitations, role checks, membership-checked workspace picker, real overview and read-only account/workspace Settings. | Guided first-run setup without technical concepts; workspace/profile/member-role editing; complete agency/client/multibrand administration and end-user acceptance. |
| Smart Sources and storage connectors (03–04) | Explicit cloud-folder/local-companion scope, Drive/Graph adapters, cursor reconciliation, webhook/polling machinery, stabilization/readiness rules, source configuration tests. | Live deployment-specific OAuth/provider acceptance and full beginner setup; automatic source-ready campaign-template binding; full provider edge-case and sustained recovery coverage. |
| Context Packs and Content Understanding (05–06) | Immutable published context versions, selected sections/facts/authority, bounded extraction, evidence/conflict-aware packages, Learning Review corrections, deterministic draft generation. | Full multimodal understanding and grounding, retrieval quality/evaluation, OCR coverage, broad formats, and scoped correction-to-future-behavior learning. |
| Brand/audience profiles and information/promotional controls (07–08) | Versioned profiles, exact campaign pins, deterministic precedence and ceilings, audience variants and reviewed presentation successors. | Complete cross-level override experience, representative user previews/custom controls, and evidence that defaults work across all intended customer types. |
| Destinations, campaigns and scheduling (09–11) | Destination registry/tracked links; immutable DAGs; immediate, exact-time, dependency and duration waits; persisted completion delays; independent branches; half-open request-start windows for text-only Discord/Slack/Mastodon official API; guarded expiry, approvals, cancellation and historical recovery; read-only calendar. | Business-friendly templates and event-to-campaign activation, richer conditions, recurring/evergreen/follow-up scheduling, quiet hours/blackouts/collisions, action-specific pacing, and evidence-backed review waivers. Window media/multistage/fallback routes remain unsupported; any campaign mixing bounded scheduling with companion execution remains saved-only. Saved enums alone are not support. |
| Social/channel connectors and alternative execution (12–13) | Governed Discord, Slack, Mastodon and Mailchimp outbound paths, exact previews and provider results, capability checks, attended companion URL work and manual handoff. | Broader owned social/destination/commerce/subscription connectors; general authorized browser actions and resilient browser-session lifecycle; production acceptance of each provider path. Existing provider-specific approval requirements still restrict autonomous sending. |
| Discovery, relationships and unified inbox (14–15) | Relationship/suppression registry, reviewed identity links, internal threads/notes/history, ownership/handoff/deadlines, deterministic assistant suggestions, response drafts and contact context. | Production provider inbound-message synchronization, actual provider reply/send paths, saved live discovery searches, contextual outreach execution, contact-rate/fatigue enforcement across providers, and natural conversation continuity acceptance. Internal notes/drafts are not delivered messages. |
| Media/accessibility (16) | Immutable originals, bounded image/PDF/OOXML processing, Sharp derivatives, reviewed alt text and image scan/rights evidence, signed preview access and S3-compatible object boundary. | Video/audio transformation and transcription/captioning, broader accessibility automation and evaluation, watermark/codec/duration recipes, OCR and all required destination-specific media variants. |
| Analytics, attribution and experiments (17) | First-party tracked visits, normalized measurement ingestion, currency-aware outcome criteria, correction-aware Mailchimp/Mastodon aggregates and durable success signals. | Full analytics/reporting experience, lead/commerce integrations, multi-touch attribution, controlled experiments, recommendation quality, fatigue analysis and approved optimization loops. Provider aggregate counts do not establish reach, identity, sentiment, attribution or conversions. |
| Rights, governance and security (18) | Workspace authorization, encrypted vaults, reviewed image rights/account/campaign/brand scopes, scan gates, selected retention/legal-hold/erasure controls, audit and operational incident primitives. | Complete rights/expiry/geography/transformation policy across all media/actions; broader retention/export/erasure surfaces and external-system recovery; administrator session/device controls and production security acceptance. |
| AI models, agents and cost (19) | Provider-neutral policies, deterministic templates/assistants, budget quote/reservation/settlement ledger, hosted credential/contract/health controls and gated one-shot reviewed text revision. | Simple automatic task selection throughout the product, local/self-hosted model execution, broader AI/media capabilities, compliant automatic fallback, representative eval sets and model/prompt promotion gates, and real provider billing/quality acceptance. A discovered model or approved adapter record alone is not execution readiness. |
| Reliability, OS/deployment and extensibility (20–22) | Durable workflow/command/idempotency state, reconciliation/retry primitives, worker heartbeats/readiness, logical database recovery drill, container composition, Windows companion preview and selected authenticated APIs. | Complete end-to-end restore/load/failure acceptance; independent collector outage monitoring; signed distribution/updates; macOS, Ubuntu and CentOS Stream/RHEL-compatible companion/server validation; Podman validation; all named browser coverage; supported public API/webhook/export/extension SDK contracts. |

Primary implementation locations are `apps/web/src/app`, `packages/database/src`, `packages/domain/src/policies.ts`, `packages/workflows/src`, `apps/workflow-worker/src`, `packages/connectors/src`, `packages/ingestion/src`, `packages/media/src`, `packages/generation/src`, and `apps/companion`. Detailed current contracts are in [Domain Model](DOMAIN_MODEL.md); build/operations instructions are in [Developer Guide](DEVELOPMENT.md).

## What the 1.23 preview changes

- One selected approved draft variant from the original preparation generation becomes a protected executable draft in the same Campaign. The server compiler retains original profile/control pins and emits one approval-required text-only official-API step, with immediate, exact-time or bounded-window timing. No arbitrary graph, inferred account, media or automatic activation is introduced.
- One coherent raw snapshot supplies both displayed review content and a versioned fingerprint. Full canonical bytes preserve raw capability/UTM keys, Unicode/URLs and six-digit timestamps; lossy JSON numbers and runtime account aliases fail closed. Same-ID preview recreation invalidates the selection, while display labels and health timestamps do not.
- Atomic authorized finalization and immutable receipts make exact retries safe after response loss. Protected Campaigns cannot be edited into an unprotected successor. Separate exact-version publish controls avoid saving/replacing a reviewed definition; activation remains explicitly non-idempotent, with existing-run inspection after uncertainty.
- Durable provenance is rechecked at activation, pinned execution authorization and locked new/retry publication admission. Database content/GUC compatibility guards block older workers from silently bypassing proof, while accepted/uncertain history remains recoverable. Optional absent Discord guild identity and actual owned tracked-link locking received live regression fixes.
- Readiness expects migration 0112/count 112. Guided source onboarding, source-ready template binding, reusable templates, all-provider/media coverage and complete product acceptance remain open. Release-specific acceptance is recorded separately; this section is a capability map, not a production-readiness certificate.

## What the 1.22 preview changes

- Approved packages offer a writer-only short form for General Announcement v1, exact reviewed package revision, current published profile pins, optional Destination and copy controls. The result lists every initial variant and immutable source/reference evidence, without activation or sending.
- One transaction creates a `draft_only` planning Campaign/version, governed drafts, completed immutable preparation receipt and audit. Tenant/key uniqueness and full canonical payload comparison prevent duplicate campaigns after uncertain responses. Current role locks, source/profile locks and existing policy validation remain mandatory.
- Saved exact browser requests support manual check/retry after reload; an explicit new-attempt action acknowledges possible prior success. Membership-scoped Campaign receipt links recover results even after tab storage is lost. Historical receipt IDs do not follow later edits or imply current approval.
- Real regressions exposed SQL-null profile settings masking a stricter audience ceiling and split preview capability/timestamp reads admitting stale rendering. Null settings are now absent; exact raw capability snapshot/provider/timestamp checks and connection-first locking fail closed. Affected historical normalized previews require recreation/review, without deleting their records.
- Readiness expects migration 0111/count 111. The future same-Campaign executable-plan finalizer, reusable templates, automatic source-ready preparation, complete onboarding and broader specification remain open.

## What the 1.21 preview changes

- Historical claim UUIDs now resolve against their exact immutable generation snapshot even after current package evidence is replaced. Safe backfill repairs only provable missing mappings; ambiguous history remains explicitly unavailable.
- A complete factual order, unique snapshot IDs, exact captured text/provenance and tenant/version lineage govern new links. Ordinary and AI revisions preserve every fact and fail closed for missing/contradictory proof; paid AI context checks happen before intent creation.
- Shared package locking keeps generation approval/revision/evidence consistent with refresh. Old approvals remain version-bound, and new content requires its own review.
- Draft trace labels historical gaps, duplicate IDs and factual text conflicts honestly; only a nonfactual call to action receives the presentation-only label. Readiness now expects migration 0110/count 110. Coordinated writer downtime is required for schema/code rollout; mixed-version operation is not supported.

See [Evidence retention](EVIDENCE_RETENTION.md) for functions, columns, collections, lifetimes, repair limits and migration acceptance.

## What the 1.20 preview changes

- **Time comes from durable evidence.** Version-pinned UTC windows are inclusive at the start and exclusive at the end. Dependency delays use each same-instance predecessor's first durable success/partial success, never an activity retry time. PostgreSQL supplies fresh time after authorization locks; conservative monotonic request budgets prevent a slow or backwards worker clock from extending a deadline.
- **Expiry is distinct from uncertain delivery.** Undispatched expired work becomes `schedule_blocked` and pauses the instance. Resume and all manual completion are rejected; cancel and a newly reviewed plan are the initial recovery. Started writes settle or reconcile. Exact persisted successes replay without credentials, links, media or provider calls.
- **Branches and approval waits remain recoverable.** The Temporal `bounded-scheduling-v1` patch retains frozen 1.19 behavior for unmarked histories. Independent eligible branches progress without unrelated slow siblings. Three real history replays cover new and legacy paths. Deploy the new worker before enabling new web activation; retain the legacy path while histories remain in retention.
- **Publication admission is shared.** Initial claims and failed retries recheck exact identity, current approvals/content/account state and schedule under one lock boundary. One winner may send. Late preflight health writes cannot reactivate a revoked connection or overwrite a changed credential/configuration snapshot. This is not an atomic guarantee across every rights writer and external HTTP.
- **Authoring and controls expose the actual scope.** Delay values, UTC milliseconds, optionality and advanced objects survive edits. Calendar shows authored bounds and pinned evidence without inventing a reserved slot. Windows require a current publish-capable official API connection and supported text-only route; companion mixed plans, recurrence and pacing remain separate work.

See [Scheduling contracts](SCHEDULING_CONTRACTS.md) for types, maps, locks, error/recovery semantics and clock assumptions.

## Historical 1.19 preview changes

- **Current workspace is consistent.** `getActiveWorkspace` and the shared shell check membership on every request. The cookie is a hint; switching is origin-checked and membership-authorized, and only safe section roots survive navigation. Revoked or absent membership cannot reveal another tenant's records. A zero-membership account receives a stable no-access screen. Settings shows actual facts and existing Team invitation controls without fictitious save buttons.
- **Overview/calendar show persisted evidence.** Overview summarizes saved records and pending queues, not demo results or live health. Calendar reads immutable version snapshots for runs, clearly labels draft/published plans that are not queued, distinguishes fixed-time and untimed work, and reports unsupported execution semantics. It does not claim recurrence, collision prevention, scheduling optimization, or guaranteed delivery.
- **Campaign plans survive ordinary editing.** Explicit UTC fields preserve absolute instants, seconds/milliseconds, window bounds and advanced condition/input/output objects. Execution methods, optionality, retries and timeout values round-trip. Publishing/activation semantics distinguish draft from currently published versions. Unsupported fields remain editable but cannot silently become immediate actions.
- **Approval and execution authority are real checks.** Draft-only/unknown modes cannot run. Whole-campaign approval captures the exact version/steps; `__campaign__` is reserved. Queued activities check active/running state, due time, finished dependencies and current approvals; publication creation rechecks exact lineage and target-bound idempotency. Confidence/history modes retain review until their actual waiver logic exists, and stricter provider approval rules still apply.
- **Failed publication retries have one owner.** The retry comparison-and-set grants permission to one caller only. Original rendered content/subject, complete provider identity, exact approved preview and current authority are checked; losers do not send or overwrite active state. This is not a transactionally locked guarantee over all mutable media-rights reads and provider I/O; that common dispatch boundary remains production hardening.
- **Collector backlog has retained incidents and claim fencing.** Migration 0108 adds one active overdue/abandoned incident per workspace/type, five-minute thresholds, collection-equivalent eligibility, one observer snapshot and advisory locking. Automatic snapshot/completion writes carry the claim's attempt number. Recovered claims cannot be overwritten by old workers. Monitoring remains coupled to the enabled collector and does not independently detect a stopped process or deliver a pager notification.

## Bounded next roadmap

1. **Connect the beginner content-to-campaign workflow.** Add reviewed universal templates and an explicit Smart Source/ready-package binding, preview its generated steps/drafts/destinations, and persist an idempotent ready-event trigger. Acceptance: a nontechnical operator can configure a folder and context, correct its interpretation, approve a draft, and launch the intended campaign without editing JSON or losing/repeating work after restart.
2. **Extend scheduling beyond the bounded text-only scope.** Add recurrence/evergreen/follow-up and supported conditions with timezone/DST behavior, blackout/quiet-hours policy, collision reporting and durable per-action pacing. Extend deadlines to multistage media and companion only with end-to-end enforcement. Acceptance: representative time changes, dependencies, parallel branches, stops and worker restarts preserve the intended schedule and never dispatch early or duplicate completed actions.
3. **Complete one real provider inbox-to-reviewed-response flow, then expand.** Ingest authorized provider messages with stable identifiers and reconciliation, preserve relationship/context/suppression, send an explicitly reviewed response, record the provider result and handle ambiguity. Extend to bounded live discovery/outreach only with configured contact authority and pacing. Acceptance: continuity, handoff and do-not-contact behavior hold across incoming/reply/follow-up cycles.
4. **Connect useful AI and media processing to the main workflow.** Turn current model/budget controls into simple task choices with evaluated grounded outputs; support local/self-hosted execution and the required audio/video/accessibility jobs. Preserve source evidence and cost/privacy constraints. Acceptance: representative evaluation sets and actual provider/local runs prove quality, billing reconciliation, privacy location and failure behavior before promotion.
5. **Deliver measurable outcomes and operational completeness.** Add analytics views, lead/commerce events, experiment allocation/results and reviewed optimization. Finish independent monitoring, whole-system restore and load drills, retention/export and administrator controls. Acceptance: campaigns can be traced to source, approval, execution, cost and outcome through interruption/recovery without invented metrics or missing evidence.
6. **Validate supported platforms and distribute intentionally.** Produce signed/updateable companion artifacts and deployment instructions for Windows, macOS, Ubuntu and CentOS Stream/RHEL-compatible environments; exercise Docker/Podman and Chrome/Edge/Firefox/Safari as specified. Public reviewed-source publication is already authorized. Acceptance is installation, login, update, browser/local-folder job and health/recovery on each required target, not merely shared source compiling on Windows.

Provider accounts, deployment origins/credentials and operating-system hosts may require user input when those specific integration gates are reached. They do not block independent local implementation work.

## Evidence and release gates

At this checkpoint, **693 distinct TypeScript cases across 83 files** and three Rust tests have passed. The non-overlapping package totals are web 188, workflow-worker 60, companion protocol 4, connectors 166, database 109, domain 61, generation 29, ingestion 11, media 24 and workflows 41. Fresh replay applied all 110 migrations into 129 public base tables; rerun skipped all 110 with matching checksums. Full workspace typecheck, lint, the 91-page production web build, companion frontend and native Windows preview packaging passed. The historical 109-to-110 rehearsal covered eight real legacy cases. Workflow tests retain three actual history replays. Final browser/source/cloud evidence is recorded in Releases. These checks do not prove every specification acceptance criterion.

Both full and production dependency audits reported zero vulnerabilities locally; future audits may change. The 1.21 Windows installer fingerprint is in Releases and is unsigned/local-only. QA helpers intentionally modify synthetic fixture records and must run only in isolated databases; see the Developer Guide. Application database and PostgreSQL volume are preserved. Google and cloud release evidence is recorded after each corresponding verification, separately from local gates.

The approved global criteria still require universality, nontechnical usability, simple AI choices/currency costs, end-to-end traceability, consistent interruptible autonomy, restart/retry reliability, all-platform acceptance, security, capability-flexible execution, cost control, and representative quality gates. None is considered complete solely because its schema, enum, documentation, or unit tests exist. Progress continues through the broader roadmap above; the [bounded scheduling plan](SCHEDULING_PLAN.md) records the completed initial scope and its limits.
